var n : any = 1;

n = "rere";


var isWinter : boolean = false;

isWinter = 1;

var count : Number = 5;

var name : string = "Bran";

name = 'john';

var names : any[] = ["johhny", 3, "test"]

enum Starks {Jon, Bran, Eddard, Catlyn};

var cat : Starks = Starks.Catlyn;

function getName() : string {
    
    return 1;
}